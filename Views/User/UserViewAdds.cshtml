@model ProjectAK.Models.ad_view_model
@{
    ViewBag.Title = "UserViewAdds";
}

@*<h2>UserViewAdds</h2>*@

<div class="AVA-8">
    <div class="row">
        <div class="col-lg-12 col-md-12">
            <img src="" class="img-circle img-responsive" style="width:100%" />
        </div>

        <div class="col-lg-12 col-md-12">
            <div>
                <h1>@Model.pro_name @*<small>Ad-ID:@Model.pro_id</small>*@</h1>
            </div>
            <img src="@Url.Content(@Model.pro_img)" style="height:450px; width:80%;  border-radius: 20px; box-shadow: 0 5px 15px rgba(0,0,0,0.3);" />

            <table class="table-bordered table-responsive table-hover">
                <tr><td><h3>Category Name : </h3></td><td>@Model.cat_name</td></tr>
                <tr><td><h3>About :</h3></td><td>@Model.pro_desc</td></tr>
                @*<tr><td colspan="2">
                    @if (Convert.ToInt32(Session["u_id"]) == Convert.ToInt32(Model.pro_adm_id_fk))
                    {

                        <a href="@Url.Action("BookAdd", "User")">
                            <div class="btn btn-success">
                                ADD
                            </div>
                        </a>

                    }</td>
                </tr>*@
                @*<tr>
                    <td colspan="2">
                        @if (Convert.ToInt32(Session["ad_id"]) == Convert.ToInt32(Model.pro_adm_id_fk))
                        {
                        <a href="@Url.Action("BookAdd", "User")">ADD</a>
                        }
                    </td>
                </tr>*@

            </table>

            <!--<div>
            <h1>Description</h1>
            <p>@Model.pro_desc</p>
        </div>

        <div>
            <h1>Category Name</h1>
            <p>@Model.cat_name</p>
        </div>-->
            @*<div>
                <h1>Posted by: </h1>
                <p>@Model.ad_name<img src="@Url.Content(@Model.u_image)" style="width:50px;height:50px" /></p>
            </div>*@
            @*<div>
                <h1>Contact</h1>
                <p>@Model.u_contact</p>
            </div>*@
        </div>

        <div>
            <a href="@Url.Action("BookAdd", "User")">
                <div class="btn btn-success">
                    Book Meeting
                </div>
            </a>
        </div>

    </div>
</div>


