@using PagedList.Mvc
@model  PagedList.IPagedList<ProjectAK.Models.category>
@{
    ViewBag.Title = "ViewCategory";
}

@*<h2>ViewCategory</h2>*@
<div class="container" >

    @*<div class="v-hh">
            <h3>Category</h3>
        </div>
        <hr />*@
    <div class="row" style="margin-bottom:40px;">
        @if (Model != null)
        {
            foreach (var item in Model)
            {

                <div class="col-md-4" style="height:220px;margin-top:150px;">
                    <img src="@Url.Content(item.cat_img)" style="height:250px;width:100%;" />
                    <br />
                    <h4 style="color: black;font-family:'poppins';text-align:center;margin-top:20px;">@Html.ActionLink(@item.cat_name, "DisplayAdd", new { id = item.cat_id })</h4>
                    <br />
                    <div style="text-align: center">
                        @Html.ActionLink("Delete", "Delete", new { id = item.cat_id }, new { @class = "btn btn-danger btn-sm" })
                    </div>
                </div>
            }
        }
    </div>
    <div id="container">
        <div class="row" style="margin-top:150px;">



            <div class="col-md-4" style="text-align:center;">
                <div class="pagination">
                    Page @(Model.PageCount<Model.PageNumber?0:Model.PageNumber)
                    of @Model.PageCount @Html.PagedListPager(Model, page => Url.Action("ViewCategory", new { page }))
                </div>

            </div>
            <div class="col-md-4">
            </div>

            @*<div class="col-md-4" style="text-align:center;">
                <br />
                <br />
                <br />
                <a href="@Url.Action("CreateAdd", "Admin")"><div class=" btn btn-success"> Add People </div></a>
            </div>*@

        </div>
        </div>

   
    @*<div style="padding-left: 77%;">
        <a href="@Url.Action("CreateAdd", "Admin")">
            <div class=" btn btn-success">
                ADD and POST
            </div>
        </a>
    </div>*@
</div>





    @*<div class="container" style="height: 1150px; font-family: poppins">
        <div class="row">

            <h1 style="text-align: center; margin-top:5%;">PRODUCT CATEGORY</h1>
        </div>

        <div class="row" style="margin-bottom:40px;">
            @if (Model != null)
            {
                foreach (var item in Model)
                {

                    <div class="col-md-4" style="height:220px;margin-top:150px;">
                        <img src="@Url.Content(item.cat_img)" style="height:250px;width:100%;" />
                        <br />
                        <h4 style="color: black;font-family:'poppins';text-align:center;margin-top:20px;">@item.cat_name</h4>
                        <br />
                        <div style="margin-left:160px;">
                            @Html.ActionLink("Delete", "Delete", new { id = item.cat_id }, new { @class = "btn btn-danger btn-sm" })
                        </div>
                    </div>
                }
            }
        </div>



        <div style="margin-top:150px;">
            <div id="container" style="margin-left:20px">
                <p></p>
                <p></p>
                <div class="pagination" style="margin-left:400px">
                    Page @(Model.PageCount<Model.PageNumber?0:Model.PageNumber)
                    of @Model.PageCount @Html.PagedListPager(Model, page => Url.Action("ViewCategory", new { page }))
                </div>
            </div>
        </div>
    </div>*@




